# C-HotelSystem
C-HotelSystem is a compact C# application for managing hotel operations: reservations, guests, rooms, and billing. It uses JSON files for data persistence by default, follows a clear separation of concerns for testability, and is suitable for learning, demos, and small deployments.
